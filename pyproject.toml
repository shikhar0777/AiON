[project]
name = "aion"
version = "1.0.0"
description = "AI-powered global news discovery platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
pythonpath = ["."]
